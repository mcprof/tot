export EDITOR='vim'

function cd() {
    new_directory="$*";
    if [ $# -eq 0 ]; then 
        new_directory=${HOME};
    fi;
    builtin cd "${new_directory}" && ls
}

alias jim='vim ~/.bash_profile; source ~/.bash_profile'
